#include<stdio.h>
#define MAXLEN 1000
#define IN 0
#define OUT 0



int main(void){
  char line[MAXLEN];
  return 0;
}

int my_getline(char arr[], int max){
  int len = 0;
  while ((c = getchar()) != EOF && c != '\n'){
    line[len] = c;
    ++len;
  }
  return len;
}

int lineMod(char arr[]; int len):
  int i = 0;
  char e;
  while (e = arr[i] =! '\n' && e != '\0')
